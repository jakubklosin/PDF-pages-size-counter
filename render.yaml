services:
  - type: web
    name: pdf-analyzer
    runtime: python
    buildCommand: pip install -e .
    startCommand: uvicorn pdf_analyzer.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
    plan: free
